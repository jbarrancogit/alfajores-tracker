// SPA Router — implemented in Task 4
document.addEventListener('DOMContentLoaded', () => {
  document.getElementById('app').innerHTML = '<p style="color:#a1a1aa;text-align:center;margin-top:40vh;">Cargando...</p>';
});
